{"name":"Unzip project","on":{"workflow_dispatch":{}},"permissions":{"contents":"write"},"jobs":{"unzip":{"runs-on":"ubuntu-latest","steps":[{"uses":"actions/checkout@v4"},{"run":"unzip -o update.zip && rm -f update.zip && ls -R lib/data"},{"run":"git config user.name github-actions && git config user.email actions@github.com && git add -A && git commit -m apply-update && git push"}]}}}
