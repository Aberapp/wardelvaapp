{"name":"Unzip project","on":{"workflow_dispatch":{}},"permissions":{"contents":"write"},"jobs":{"unzip":{"runs-on":"ubuntu-latest","steps":[{"uses":"actions/checkout@v4"},{"run":"unzip -o wardelva_flutter.zip && shopt -s dotglob && rm -rf wardelva_app/.github && cp -r wardelva_app/* . && rm -rf wardelva_app wardelva_flutter.zip"},{"run":"git config user.name github-actions && git config user.email actions@github.com && git add -A && git commit -m unzip && git push"}]}}}
